# Lab Wizard Flutter Improvement Roadmap

This is the authoritative checklist for the post-1.0.4 Android improvement program. Items are implemented in ID order unless a prerequisite requires otherwise. An item is only marked complete after source validation, automated tests, a successful signed GitHub APK build, and phone verification where applicable (see [docs/PHONE_VERIFICATION_CHECKLIST.md](docs/PHONE_VERIFICATION_CHECKLIST.md) for the physical device acceptance protocol).

Status legend: `TODO`, `IN PROGRESS`, `BLOCKED`, `VALIDATING`, `RELEASED`.

Ground rules for every item:

- The Next.js web app in `src/` and the Supabase schema it relies on are never broken. Database changes are additive only (new nullable/defaulted columns, new tables, new RPCs, new indexes) and live in numbered files under `flutter_app/supabase/`.
- The Flutter app keeps working against the current schema when an optional migration has not been applied yet.
- Verification runs on GitHub, not on developer machines: `.github/workflows/flutter-branch-ci.yml` formats, auto-fixes, analyzes, tests, and builds a signed APK artifact for every branch push; `android-apk.yml` publishes releases from `main`.

## Milestone A — Large-inventory speed and daily usability

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| UX-01 | RELEASED | Compact/detailed inventory mode | User can switch modes from either shelf; preference persists; compact rows retain quantity, status, details, use/damage and restock access. | None |
| UX-02 | RELEASED | Alphabetical quick navigation | A–Z control jumps to the first matching item; unavailable letters are visibly disabled; works after sorting/searching. | UX-01 |
| UX-03 | RELEASED | Sticky search and filters | Collapsing shelf heading; search/filter/sort controls remain reachable while scrolling without hiding most of the list. | UX-01, UX-02 |
| UX-04 | RELEASED | Undo inventory actions | Recent consume/restock/damage can be reversed safely; reversal is represented in the audit trail and works with offline outbox retries. | SYNC-01 |
| BATCH-01 | RELEASED | Batch restock | Multi-select chemicals/apparatus, validate amounts, show progress and record separate audit entries. | None |
| BATCH-02 | RELEASED | Batch threshold update | Multi-select items and set low-stock values with preview and validation. | None |
| BATCH-03 | RELEASED | Batch category/location assignment | Apply supported shared fields to selected items; incompatible fields are not silently changed. | DATA-01 |
| BATCH-04 | RELEASED | Safe multi-delete | Selection summary, typed confirmation for large deletions, online requirement and clear audit/export warning. | None |
| QR-01 | RELEASED | Selected-item QR printing | Select specific chemicals/apparatus and print only those labels; retain 40-per-A4 option. | None |

## Milestone B — Import and higher-quality data entry

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| IMPORT-01 | RELEASED | Android CSV import | File selection, column mapping, preview, validation, duplicate warnings, partial-error report and confirmed import. | DUP-01 |
| DUP-01 | RELEASED | Duplicate detection | Normalized name/formula/category checks; view existing or explicitly add anyway. | None |
| FORM-01 | RELEASED | Remember form preferences | Persist recent unit, category, action amount and preferred threshold behavior per user/device. | None |
| DATA-01 | RELEASED | Additive chemical metadata | Optional supplier, CAS, concentration, location, expiry and hazard fields; old web/app rows remain valid. | Additive SQL migration |
| DATA-02 | RELEASED | Expiry and hazard presentation | Shelf/detail indicators, filters and report support without overwhelming normal cards. | DATA-01 |

## Milestone C — Apparatus operations

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| GEAR-01 | RELEASED | Serialized apparatus metadata | Serial number, condition, assigned person, purchase date and warranty expiry. | Additive SQL migration |
| GEAR-02 | RELEASED | Checkout and return | Clear availability, borrower, due date, return condition and immutable history. | GEAR-01 |
| GEAR-03 | RELEASED | Maintenance and calibration | Due dates, completion records, notes and status warnings. | GEAR-01 |
| GEAR-04 | RELEASED | Apparatus history | Unified checkout, return, damage, maintenance and calibration timeline/report. | GEAR-02, GEAR-03 |

## Milestone D — Notifications

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| NOTIFY-01 | RELEASED | Notification preferences | Per-type controls, permission education and Settings management. Settings → *notifications*: master switch asks for the Android 13+ permission, blocked state explained with an *open system settings* button, per-topic switches, warning windows, reminder hour and a test notification; stored on the device only. | None |
| NOTIFY-02 | RELEASED | Low-stock and expiry alerts | Deduplicated local alerts with sensible timing and direct item navigation. Alerts fire once per day per item (persisted), more than three at once collapse into one digest, expiry reminders are scheduled ahead (N days before + expiry day) so they arrive with the app closed, and tapping opens the item sheet. | DATA-01, NOTIFY-01 |
| NOTIFY-03 | RELEASED | Return/calibration alerts | Overdue checkout, maintenance and calibration notifications. Loan due-time reminders, overdue-return alerts, service *due soon* / *due today* / *overdue* alerts; all open the apparatus sheet. | GEAR-02, GEAR-03, NOTIFY-01 |
| NOTIFY-04 | RELEASED | Sync and weekly summary alerts | Stale outbox warning and optional weekly stock summary. Failed outbox changes raise one alert that opens the sync center; the optional weekly summary repeats on a chosen weekday/hour and the *needs attention* screen shows the same digest text in the app. | SYNC-01, NOTIFY-01 |

## Milestone E — Offline and synchronization reliability

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| SYNC-01 | RELEASED | Sync center | List pending/failed operations, attempts, error, retry one/all, safely discard where allowed, and last successful sync. | Local DB migration |
| SYNC-02 | RELEASED | Incremental synchronization | `updated_at` cursor, pagination, tombstone/deletion handling, full-resync escape hatch and per-user isolation. Migration 007 (additive: `updated_at` + triggers + `deleted_rows`); keyset paging on `(updated_at, id)` in pages of 500 with a 2-minute overlap; tombstones applied per table; automatic full download when cursors are missing or older than 120 days; **Download everything again** in the sync center; per-user cursors and a defensive `user_id` filter; paged full downloads on servers without the migration. | Additive SQL migration |
| SYNC-03 | RELEASED | Background synchronization | Connectivity/app-resume triggers and Android background scheduling without duplicate actions or battery abuse. Foreground: sync on app resume (skipped when a download happened < 2 min ago and nothing is queued) and when the connection returns. Background (WorkManager): a periodic job every 1/3/6/12/24 h (user setting, default hourly, optional Wi-Fi only, battery-not-low) that sends the outbox and downloads changes, plus a one-off *flush* job scheduled when the app is left with queued changes (runs when a network is available, exponential retry). No duplicate actions: a job that fires while the app process is alive hands the work to the main isolate over `IsolateNameServer` (single Supabase session, so refresh-token rotation cannot sign the user out); only when nobody answers does the job start its own client from the persisted session. The outbox itself is idempotent (`operation_id`). Settings card (toggle, frequency, Wi-Fi only, last run + result) and a sync-center card; jobs are cancelled on sign-out. | SYNC-01, SYNC-02 |
| SYNC-04 | RELEASED | Conflict resolution | Explain server/local conflicts and offer safe resolution; never silently overwrite unrelated newer data. Edits send only the fields that actually changed and are applied as a compare-and-set (PostgREST filters on the last-seen values, no server change): unrelated newer server data is never touched, and a field changed on both sides becomes a *conflict* — live in the edit form (dialog: keep server values / overwrite with mine / cancel) or, for a queued change, in the sync center (badge, plain-language explanation with both values, *keep server values*, *overwrite with mine*, *discard*). Stock conflicts (server has less than a queued consume/damage needs, or an undo the server cannot honour) explain the amounts and offer *apply what is left* where that makes sense; a deleted item is explained and can only be removed. Conflicts are never retried blindly (*retry all* skips them) and survive restarts (local DB v4 adds an outbox `conflict` column). Servers without the atomic RPC get the same protection: quantity writes are rebased on the server's value instead of overwriting it. | SYNC-02 |

## Milestone F — Scanner and QR workflow

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| QR-02 | RELEASED | Bulk apparatus QR labels | A4 output and selected-item support using stable apparatus identifiers. Apparatus labels encode the row id (`labwizard:apparatus:<id>`, unchanged by renames) and print the name, category, serial number and a short id for eye-matching; the apparatus shelf gets *QR labels for all* in the menu and *labels* in the selection bar, sharing one A4 sheet builder with chemicals (grid choice 40 / 24 / 12 per page, page count preview, print or share the PDF). | QR-01 |
| QR-03 | RELEASED | Save/share individual label | Export a clean label image/PDF from item details. The item sheet shows the label as it will be exported (QR left, name/details right) with *share image* (PNG through the system share sheet, so it can be saved, messaged or printed), *PDF* (single 80 × 50 mm page) and *print*. | None |
| SCAN-01 | RELEASED | Recent scans | Device-local, user-scoped list with direct item access and clear-history control. The scanner page keeps the last 30 codes per signed-in user in shared preferences (`scans.<user id>`, never uploaded): matched items open their sheet with one tap, items deleted since are marked *removed*, unknown codes are kept as *not matched* with a copyable payload, and *clear* asks before forgetting the list. Scan matching moved into a pure `resolveScan` function. | None |
| SCAN-02 | RELEASED | Continuous batch scanning | Scan multiple labels without reopening camera; duplicate handling and completion summary. A *Single / Batch* switch under the camera: in batch mode every recognised label is added to a `ScanBatch` while the camera keeps running, a repeated label increments its count (with a lighter haptic and an *already in batch ×n* message) instead of appearing twice, the same code re-read within two seconds is ignored, unknown codes are kept for the summary, and *Finish (n)* opens a summary sheet (items with counts, unknown codes, open item, *Keep scanning* or *Done*). | SCAN-01 |
| SCAN-03 | RELEASED | Scan-and-act | After recognition, enter quick amount and perform consume/restock/return without unnecessary navigation. A recognised label (single mode) opens a compact sheet: name, stock and loans at a glance, one amount field (remembered per FORM-01, whole pieces for apparatus) with one-tap chips, *Use* / *Restock* / *Damage*, and *Return (n out)* when the apparatus has open loans (asks which loan only if there are several). Success closes the sheet, flashes the result over the camera and offers *Undo* in a snackbar; validation and stock errors stay inline; *Open details* leads to the full sheet. | GEAR-02 for returns |
| SCAN-04 | RELEASED | External barcode support | Optional EAN/Code formats, explicit item association and no unsafe automatic matching. Additive `008_external_barcodes.sql` adds a nullable `barcode` column to `chemicals` and `apparatus`. The scanner has an off-by-default *Also read product barcodes* switch (EAN-13/8, UPC-A/E, Code 128/39/93, ITF, Data Matrix); a code opens an item only when it equals that item's linked `barcode`, never by lookup or guess. An unknown code (single mode, or later from *recent scans*) opens a *link this code* sheet: search, pick the item, the link is written through the normal offline-capable update path, a code that was on another item is moved, replacing an item's existing code asks first. The item sheet shows *Linked barcode …* with *unlink*. | DATA-01 |

## Milestone G — Reports and exports

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| REPORT-01 | RELEASED | Flexible date ranges | 7-day, 30-day, monthly and custom inclusive ranges with correct local-day handling. `ReportRange` (inclusive local calendar days; membership decided on the log's local date, day arithmetic that survives daylight-saving changes, `previous` period of equal length or previous calendar month) drives the report page: *7 days / 30 days / month / custom* chips, month arrows, tap the range to open a Material date-range picker, label + exact dates + day count. The activity chart buckets per day up to 62 days and per week beyond, with tooltips; the PDF title and file name carry the range. | None |
| REPORT-02 | RELEASED | Usage/restock trends | Separate action counts and unit-aware quantities; comparison with previous period. `summarizePeriod` / `TrendComparison` count usage, restocks and damage separately, sum quantities per unit (350 mL · 20 g, never mixed; apparatus in pieces) and compare each count with the period of equal length before (previous calendar month for month ranges): +50 % / −40 % with a coloured arrow, *none before* when the earlier period had nothing (no ratio against zero), a note naming the comparison dates. | REPORT-01 |
| REPORT-03 | RELEASED | Run-out estimates | Explainable estimate based on adequate history; no estimate when data is insufficient. `estimateRunOut` uses only consume entries of the last 90 days and requires 3+ uses spanning 7+ days; the pace is total used ÷ days of history, the estimate is stock ÷ pace, and every row states its reasoning (*40 mL left; 3 uses totalling 30 mL over the last 20 days, about 1.5 mL per day*). Items without adequate history are counted, never guessed (*4 items have too little history (need 3+ uses over 7+ days …)*). Soonest first, urgency colours at ≤ 7 / ≤ 30 days, *already out* / *today* wording, independent of the selected report range. | REPORT-01 |
| REPORT-04 | RELEASED | Expiry and apparatus reports | Expiry, damage, overdue checkout, maintenance and calibration views. The chemical report gains *expiry* (expired, then expiring within 30 days, soonest first, with *N expire later · M without a date*) and *damage* (breakage in the selected range grouped per item: incidents, amount, last date); the apparatus report gains *damage*, *overdue loans* (open loans past due, most overdue first, *N of M open loans overdue*) and *maintenance & calibration* (overdue or due within 14 days, plus open and completed-in-range counts). The same views are appended to the PDF as tables. | DATA-01, GEAR-04 |
| REPORT-05 | RELEASED | CSV/Excel-friendly exports | Formula-injection-safe inventory and log exports with selected date range. Shared `core/utils/csv.dart` writer: RFC 4180 quoting, apostrophe guard for cells starting with `=` `+` `-` `@` tab or CR (numbers stay numeric), CRLF, UTF-8 byte-order mark. The report page shares *Activity CSV* (entries of the selected shelf and range: date, time, item, formula/category, action, amount, unit, note, entry id, UTC timestamp) and *Inventory CSV* (the shelf with all DATA-01 / GEAR-01 columns, stock state, barcode). The Settings backup export uses the same writer, and the importer strips the guard again on the way back. | REPORT-01 |
| REPORT-06 | RELEASED | Branded PDFs | Optional lab name/logo/contact fields and clean print layout. A device-local *report branding* card in Settings (lab or institution name, contact line, address, optional PNG/JPEG logo copied into the app's private folder, ≤ 2 MB) feeds `buildReportPdf`: A4 with a header on every page (logo, name, contact/address, report title, range), summary cards with counts, per-unit quantities and change vs the previous period, stock-health line, activity table, run-out estimates with their basis, expiry / damage / overdue loans / maintenance views, and a footer with *Page x of y* and the generation stamp. Empty branding prints plain *Lab Wizard* reports. | Local profile (device) |

## Milestone H — Account, security and distribution

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| ACCOUNT-01 | RELEASED | Complete password recovery | Supabase allowlisted redirect, Android deep link, recovery screen and successful password update test. *Forgot password?* on the sign-in card asks for the email and calls `resetPasswordForEmail` with the app deep link `com.labwizard.labwizard://reset-password` (registered in AndroidManifest.xml; must be added to the Supabase project's redirect URLs — see the README). Opening the emailed link on the phone lets supabase_flutter exchange it for a session and emit `passwordRecovery`; the app then shows *choose a new password* (confirmation field, inline errors, *Not now*) and calls `updateUser`. The confirmation copy is the same whether or not the address exists; rate limits become *Too many attempts*. | Supabase Auth URL configuration |
| ACCOUNT-02 | RELEASED | Change password | Authenticated re-entry/validation and clear success/error states. *Change password* in Settings → *your account* opens a sheet asking for the current password, the new one (6+ characters, different from the current) and a confirmation, plus *Also sign out my other devices*. The current password is proven with a fresh `signInWithPassword` before `updateUser`; a wrong one reads *The current password is not right.*, other server errors stay inline, success closes the sheet with *Password changed.* and the optional `signOut(scope: others)` revokes other sessions while this phone stays signed in. | None |
| ACCOUNT-03 | RELEASED | Data export and account deletion | Export first, explicit destructive confirmation, server-side deletion path and clear retention behavior. Settings → *danger zone* → *Delete account…* opens a two-step sheet: first what will go (counts of chemicals, apparatus, log entries, open loans and tasks, the sign-in) with *Export my data first* (four CSV files: chemicals, apparatus, chemical and apparatus activity) and the retention note; then *Type DELETE* plus the current password. `AuthController.deleteAccount` re-authenticates, calls the additive `public.delete_my_account()` SECURITY DEFINER function from `009_account_deletion.sql` (deletes the caller's rows in every table, tombstones, then the auth user — only ever `auth.uid()`), wipes the offline copy, ends the session and shows *Your account and all its data were deleted.* Offline and missing-function cases are refused with clear messages. | Secure server function (`009_account_deletion.sql`) |
| ACCOUNT-04 | RELEASED | Active sessions/devices | Display sessions where supported and provide sign-out-all control. Capability review: Supabase Auth exposes no per-device session list to client apps (only the admin API), but supports scoped sign-out (`local`, `others`, `global`). Settings therefore shows a *sessions & devices* card describing this device (signed in when, account since, sign-in method, email confirmed) with an honest note, plus *Sign out other devices* (others scope, this phone stays in) and *Sign out everywhere* (global; also clears this phone), each behind a confirmation. The account card's button is now explicitly *Sign out (this phone)* and uses the local scope so other devices and the web app stay signed in. | Supabase/API capability review (done) |
| SECURITY-01 | RELEASED | Optional biometric/PIN app lock | Local secure storage, fallback behavior and no false claim of server-side encryption. Settings → *app lock*: a 4–8 digit PIN (stored only as a salted PBKDF2-HMAC-SHA256 hash in `flutter_secure_storage`, keyed per account), an optional fingerprint/face/phone-screen-lock shortcut through `local_auth` (the PIN keypad is always the fallback), a lock-again timeout (immediately / 1 / 5 / 15 min after going to the background; always locked on a cold start), *Lock now*, change PIN and turn off (both need the current PIN). Five wrong PINs start a 30 s cool-down that doubles up to 10 min and survives restarts. *Forgot PIN?* on the lock screen signs this phone out (account password needed again). The lock overlay sits above the navigator so sheets and dialogs are covered too, and the app is hidden from touch, focus and TalkBack while locked. Documented honestly: it only gates the app on this phone, does not encrypt the SQLite copy or anything in Supabase, and the app switcher may still show the last screen. | Secure storage dependency (added) |
| RELEASE-01 | RELEASED | Private production signing | Dual-mode Gradle signing in `build.gradle.kts` automatically loads private keystore from GitHub Secrets or `key.properties`, falls back cleanly to development key when absent. Complete generation, backup, and Play upload key recovery guide in `docs/RELEASE_AND_PLAY_STORE_GUIDE.md`. | Owner-created private key/secret access |
| RELEASE-02 | RELEASED | Google Play distribution | Release pipeline builds Android App Bundle (`.aab`) alongside universal APK; Play App Signing strategy, tested upgrade path, declared permissions, and comprehensive Google Play Data Safety answers documented in `docs/RELEASE_AND_PLAY_STORE_GUIDE.md`. | RELEASE-01, Play Console access |

## Milestone I — Accessibility, device quality and observability

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| A11Y-01 | RELEASED | TalkBack audit | Meaningful labels/order for custom controls, charts, scanner, stock status and actions. | None |
| A11Y-02 | RELEASED | Large-text support | No clipping at Android maximum practical font scale; scroll/flexible layouts where required. | None |
| A11Y-03 | RELEASED | Small phone/landscape/tablet layouts | Tested widths, responsive density and no overlaps across supported orientations. | UX-01, UX-03 |
| A11Y-04 | RELEASED | Contrast/color-blind support | Status never depends only on color; contrast audit; patterns and labels remain available. | None |
| A11Y-05 | RELEASED | Touch and reduced-motion audit | Minimum targets, predictable focus, no essential animation and all motion respects preference. | None |
| OBS-01 | RELEASED | Privacy-aware crash diagnostics | Opt-in/transparent collection, scrub sensitive inventory values and document retention. | Service selection/privacy review |

## Milestone J — Automated quality gates

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| TEST-01 | RELEASED | Golden screenshot tests | Main screens, light/dark, compact/detailed and representative empty/loaded states. | UX-01 |
| TEST-02 | RELEASED | Navigation paint regression | Automated assertion that inactive tabs are offstage and cannot paint or receive input. | None |
| TEST-03 | RELEASED | Responsive/font tests | Small width, tablet, landscape and large text overflow checks. | A11Y-02, A11Y-03 |
| TEST-04 | RELEASED | Offline/outbox integration tests | Add/update/action/retry/idempotency/conflict paths and per-user isolation. | SYNC-01, SYNC-02 |
| TEST-05 | RELEASED | Scanner and QR tests | QR routing, malformed codes, apparatus labels, batch scanning and generated PDF structure. | QR-02, SCAN-02 |
| TEST-06 | RELEASED | Large-data performance tests | 500–1,000 items, search/sort/filter/scroll and report responsiveness with budgets. | UX-01, SYNC-02 |

## Milestone K — Multi-lab and multi-organization

| ID | Status | Improvement | Acceptance criteria | Dependencies |
|---|---|---|---|---|
| ORG-01 | RELEASED | Multi-tenant schema migration | Additive `010_multi_lab_organizations.sql` (`organizations`, `labs`, `organization_members`, `lab_members`, nullable `organization_id`/`lab_id` on items and logs), dual-mode permissive RLS policies keeping single-user web app and personal rows 100% compatible. | Additive SQL |
| ORG-02 | RELEASED | Domain models and lab state | Organization, Lab, Role models (`manager`, `member`, `viewer`), `activeLabProvider`, persistence of last selected lab per account with graceful personal-mode fallback. | ORG-01 |
| ORG-03 | RELEASED | Multi-tenant local cache scoping | LocalDatabase schema v5: `cache_records` and `outbox` tagged with `organization_id` and `lab_id`; queries scoped to active lab while personal items remain private. | ORG-02 |
| ORG-04 | RELEASED | Lab switcher and organization settings | Notebook header lab selector chip, modal lab switcher sheet, organization card in settings with role badges and atomic organization setup dialog. | ORG-02, ORG-03 |
| ORG-05 | RELEASED | Automated multi-lab tests | Unit tests for domain models and role permissions, SQLite v5 upgrade and multi-tenant cache isolation tests, and widget tests for the switcher sheet and settings card. | ORG-01..04 |

## Status log

| Date | Item | Change | Notes |
|---|---|---|---|
| 2026-09-21 | UX-01, UX-02 | `VALIDATING` → `IN PROGRESS` | Audit of `main` (700020e) found neither feature in `flutter_app/lib`; both are implemented as part of this program. |
| 2026-09-21 | CI | Added `flutter-branch-ci.yml` | Branch pushes are formatted, auto-fixed, analyzed, tested and built on GitHub; APK artifacts are kept for 7 days for phone verification. Android `versionCode` is now minutes-since-epoch in both workflows so branch builds and releases install over each other as updates. |
| 2026-09-21 | UX-01, UX-02, UX-03 | `IN PROGRESS` → `VALIDATING` | Compact/detailed shelf mode, A–Z quick navigation and collapsing heading with sticky controls implemented with widget tests; awaiting green branch CI + phone check of the branch APK. |
| 2026-09-21 | SYNC-01 | `TODO` → `VALIDATING` | Local DB v2 (outbox status/label/last attempt + `sync_meta`), per-change failure handling (connectivity keeps `pending`; other errors mark `failed` and do not block other items), sync center screen with retry one/all + safe discard, last-successful-sync shown in Settings/Dashboard. SQLite-backed tests run on CI. Pulled ahead of UX-04 because UX-04 depends on it. |
| 2026-09-21 | UX-04 | `TODO` → `VALIDATING` | Undo from the post-action snackbar and from item history (entries ≤ 7 days). Mirrors the web app's undo (restore quantity, delete entry) plus an additive `inventory_reversals` table + idempotent `undo_inventory_action` RPC (`flutter_app/supabase/002_undo_inventory_action.sql`) so the history shows "undone" entries; web-parity fallback when the migration is absent. Queued-but-unsynced actions are cancelled; offline undos are queued as `undo_action` and can be discarded from the sync center with a full local rollback. |
| 2026-09-21 | BATCH-01, BATCH-02, BATCH-04, QR-01 | `TODO` → `VALIDATING` | Shared multi-select mode on both shelves (long-press or overflow "select items…", select/clear shown, back leaves selection). Bottom bar: batch restock (per-item amounts, same-amount fill, validation, progress, one audit entry per item, retry failed rows), batch threshold (old → new preview, unchanged items skipped), QR labels for the selected chemicals (same 40-per-A4 sheet), safe delete (summary with history counts, export warning, unsynced items blocked, offline blocked, typed `DELETE` for 5+ items, per-item failure report). BATCH-03 stays TODO until DATA-01. |
| 2026-09-21 | DUP-01, FORM-01 | `TODO` → `VALIDATING` | Add sheet compares normalized name/formula (chemicals) and name/category (apparatus) with the shelf, shows matches inline with *view* / *add anyway*, and blocks saving until acknowledged. Unit, category, low-stock level and action amounts are remembered per user + device (SharedPreferences) and prefilled; both prefills are switchable and forgettable in Settings. Widget + unit tests added. |
| 2026-09-21 | DATA-01, DATA-02 | `TODO` → `VALIDATING` | Additive `flutter_app/supabase/003_chemical_metadata.sql` (nullable `supplier`, `cas_number`, `concentration`, `location`, `expiry_date`, `hazard_classes` + partial index). Model, cache, search and CSV export carry the fields; add/edit sheets get a collapsible *more details* section (CAS check digit, GHS chips, YYYY-MM-DD or date picker). Expired / ≤ 30-day chemicals show a badge on cards and compact rows and an *expiring* shelf filter; the item sheet shows the details, expiry copy and hazard chips. Metadata is only sent when changed and a missing column produces a migration hint instead of a raw error. |
| 2026-09-21 | BATCH-03 | `TODO` → `VALIDATING` | Selection bar gains *location* (chemicals) / *category* (apparatus) with an old → new preview, one-tap suggestions from existing locations and unchanged rows skipped. The field is bound to the shelf so incompatible fields can never be written. Needs migration 003 for chemicals; a missing column is reported per row instead of failing silently. |
| 2026-09-21 | IMPORT-01 | `TODO` → `VALIDATING` | `file_selector`-based CSV import from Settings: RFC 4180 parser (quotes, CRLF, BOM, `;`/tab sniffing), header auto-mapping with manual dropdowns, shelf selector or `type` column, per-row validation (errors vs warnings), duplicate detection against the shelf and within the file (skip by default, opt-in import), sequential import with progress, and a report of every skipped/failed line that can be shared. Unit + widget tests cover parser, mapping, validation and the screen flow. |
| 2026-09-21 | GEAR-01 | `TODO` → `VALIDATING` | Additive `flutter_app/supabase/004_apparatus_metadata.sql` (nullable `serial_number`, `condition`, `assigned_to`, `location`, `purchase_date`, `warranty_until` + partial index). Model/cache/search/CSV export/import carry the fields; add/edit sheets get a collapsible *more details* section (condition dropdown, date entry/picker with strict YYYY-MM-DD validation); item sheet shows details with warranty copy; shelf rows mark needs-repair/fair/retired condition, assignee and warranty ending. Metadata only sent when changed; missing column → migration hint. |
| 2026-09-21 | GEAR-02 | `TODO` → `VALIDATING` | Additive `flutter_app/supabase/005_apparatus_checkouts.sql` (`apparatus_checkouts` table, RLS own rows, cascade from `apparatus`, unique `(user_id, operation_id)`). One row per loan with `quantity`/`returned_quantity`; partial returns raise the returned count and the loan closes when everything is back. Item sheet: availability line, *Check out* (person with recent-name chips, whole pieces ≤ available, optional due date, note), open loans with due/overdue copy and per-loan *return*, recent returns. Shelf rows mark *N out* / *overdue*. Stock counts are never changed by a loan. Offline: `checkout_apparatus` / `return_apparatus` outbox entries, discard restores the previous state and drops dependent returns. SQLite + widget tests added. |
| 2026-09-21 | GEAR-03 | `TODO` → `VALIDATING` | Additive `flutter_app/supabase/006_apparatus_maintenance.sql` (`apparatus_services`: kind maintenance/calibration, title, note, `due_at`, `completed_at`, `performed_by`, `result`; RLS own rows, cascade, unique `(user_id, operation_id)`). Item sheet section with *Schedule* (kind toggle, title, due date + interval chips, note), open tasks soonest first with due/overdue copy and *done*, completed history. Completion sheet records date (not in the future), performer with recent-name chips, result chips, note and an optional next task of the same kind. Shelf rows mark *calibration overdue* / *maintenance due* (≤ 14 days). Offline `schedule_service` / `complete_service` outbox entries with discard rollback (a discarded schedule also drops its queued completion). SQLite + widget tests added. |
| 2026-09-21 | GEAR-04 | `TODO` → `VALIDATING` | Pure `buildApparatusHistory` merges logs, undo reversals, checkouts/returns (late flag) and scheduled/completed tasks into one newest-first timeline; item sheet history shows the mix (latest 8) plus *Full history & report*; new `ApparatusHistoryScreen` with summary card, kind filters and a shareable plain-text report (no CSV, so no formula injection). No database change. Unit + widget tests added. |
| 2026-09-21 | NOTIFY-01, NOTIFY-02, NOTIFY-03, NOTIFY-04 | `TODO` → `VALIDATING` | `flutter_local_notifications` + `timezone`: preferences card (master switch with permission education, per-topic switches, reminder hour, weekly summary weekday), local low-stock/expiry alerts deduplicated per item per day, overdue checkout / maintenance / calibration reminders scheduled ahead, failed-outbox alert opening the sync center, optional weekly summary, and an in-app *needs attention* screen with the same content. |
| 2026-09-21 | SYNC-02 | `TODO` → `VALIDATING` | Additive migration 007 (`updated_at` + touch triggers, `deleted_rows` tombstones); per-user `(updated_at, id)` cursors in `sync_meta` (local DB v3), keyset paging of 500 with a 2-minute overlap, tombstone application, automatic full download when cursors are missing/old, **Download everything again**, legacy fallback with a hint when the server lacks the migration. |
| 2026-09-21 | SYNC-03 | `TODO` → `VALIDATING` | `workmanager` periodic job (1–24 h, Wi-Fi-only option, battery-not-low) + one-off outbox flush when the app is backgrounded with queued changes; resume/connectivity foreground triggers; main-isolate delegation over `IsolateNameServer` so the persisted Supabase session is only ever used by one live isolate; standalone background client only when the app process is gone. Settings + sync-center cards show the last run; tests cover preferences, bookkeeping, scheduling, triggers and the isolate bridge. No database change. |
| 2026-09-21 | SYNC-04 | `TODO` → `VALIDATING` | Field-level compare-and-set updates (only changed fields, guarded by their last-seen values through PostgREST filters; array fields checked against a fresh copy), `SyncConflict` model (changed / deleted / stock) persisted in the outbox (local DB v4, additive column), conflict card in the sync center with explanations and kind-specific decisions, edit-form dialog for live conflicts, RPC error mapping (insufficient stock, item not found, undo refused), *apply what is left* for stale consume/damage entries, and rebased quantity writes on the legacy (no-RPC) path. Fake-PostgREST repository tests, SQLite migration tests and widget tests added. No database change. |
| 2026-09-21 | QR-02, QR-03 | `TODO` → `VALIDATING` | Shared label module (`lib/features/labels`): `LabelSpec` (chemical = web-compatible `qr_code` payload, apparatus = stable row id + short id), A4 sheet builder with 40/24/12-per-page grids and a print-or-share dialog, single-label 80 × 50 mm PDF, PNG renderer with on-screen preview; apparatus shelf menu + selection bar get labels; item sheet gets share image / PDF / print. Unit tests for payloads, page counts and generated PDFs, widget tests for the dialog, the apparatus shelf and the item sheet. No database change. |
| 2026-09-21 | SCAN-01 | `TODO` → `VALIDATING` | `resolveScan` (pure matcher for chemical/apparatus/legacy payloads), `RecentScan` + `recentScansProvider` (per-user shared-preferences history, deduped per item, capped at 30, merged safely with scans made while loading), `RecentScansSection` on the scanner page (open item, removed/unknown states, show all, confirmed clear). Unit tests for the matcher, JSON round trip, dedupe/cap, per-user persistence and restore; widget tests for the section. No database change. |
| 2026-09-21 | SCAN-02 | `TODO` → `VALIDATING` | `ScanBatch` (pure model: added / duplicate / unknown outcomes, counts, summary line), batch mode + finish button on the scanner page, `BatchSummary` sheet. Unit tests for the model and widget tests for the summary sheet (counts, repeated reads, open item, keep scanning / done). No database change. |
| 2026-09-21 | SCAN-03 | `TODO` → `VALIDATING` | `ScanActionForm` / `showScanActionSheet` (quick amount, chips, use / restock / damage / return with loan picker, inline errors, undo snackbar, open details), wired into single-mode scanning; every entry is logged with the note *via scanner*. Widget tests cover chemical use with remembered amount and undo, inline stock errors, apparatus returns with and without a loan picker. No database change. |
| 2026-09-21 | SCAN-04 | `TODO` → `VALIDATING` | `supabase/008_external_barcodes.sql` (nullable `barcode` on both item tables, partial indexes), `barcode` on `Chemical`/`Apparatus` (parsed from server and cache, sent only when set), `findLinkedBarcode` + resolver fallback that never matches Lab Wizard payloads, `productBarcodesProvider` (device preference, off by default) with the scanner decoding product formats only when enabled, `LinkBarcodeForm` / `showLinkBarcodeSheet`, `LinkedBarcodeRow` in the item sheet, *Link to item…* from the recent-scan dialog. Tests: model round trip, resolver rules, preference persistence, link / move / replace / migration-hint flows, unlink from the item sheet. |
| 2026-09-21 | REPORT-01 | `TODO` → `VALIDATING` | `lib/features/reports/domain/report_range.dart` (`ReportRange`, `ReportBucket`, `bucketize`), report page range chips / month arrows / date-range picker, day-or-week activity chart, range-aware PDF name and title. Unit tests for boundaries (23:59 / 00:00), zone conversion, previous ranges, months incl. leap February, custom ordering and labels, bucketing; widget test switching ranges. No database change. |
| 2026-09-21 | REPORT-02 | `TODO` → `VALIDATING` | `lib/features/reports/domain/report_stats.dart` (`ActionTotals`, `PeriodStats`, `summarizePeriod`, `TrendComparison`, `formatChange`); metric cards show count, per-unit quantities and change vs the previous period with accessible labels. Unit tests for per-unit sums, apparatus pieces, previous-period maths and wording; widget test asserts quantities and +200 % after switching ranges. |
| 2026-09-21 | REPORT-03 | `TODO` → `VALIDATING` | `lib/features/reports/domain/runout.dart` (`RunOutResult` sealed: `RunOutEstimate` / `RunOutMissing(gap)`, `runOutReportForChemicals` / `…ForApparatus`), *run-out estimates* card on the report page with show-all toggle and gap summary. Unit tests for pace / days / date / wording, the three insufficiency reasons, boundary spans, empty and urgent stock, sorting and gap counts; widget test asserts the explanation text. |
| 2026-09-21 | REPORT-04 | `TODO` → `VALIDATING` | `lib/features/reports/domain/asset_reports.dart` (`expiryReport`, `damageReport`, `loanReport`, `serviceReport`, `relativeDays`), `asset_report_sections.dart` (`AssetReports.compute` + cards), PDF tables. Unit tests for each view (ordering, boundaries, range filtering, wording); widget test checks the chemical and apparatus shelves show their cards. No database change. |
| 2026-09-21 | REPORT-05 | `TODO` → `VALIDATING` | `lib/core/utils/csv.dart` (`csvCell`, `csvDocument`, `csvBytes`, `csvUnguard`), `lib/features/reports/domain/csv_exports.dart` (`activityCsv`, `chemicalsCsv`, `apparatusCsv`), report-page share buttons via `share_plus`; Settings export and CSV import switched to the shared helpers. Tests: injection payloads, quoting rules, numbers, BOM/CRLF round trip through the import parser, range/kind filtering, metadata columns. |
| 2026-09-21 | REPORT-06 | `TODO` → `VALIDATING` | `LabProfile` + `labProfileProvider` (SharedPreferences JSON, logo file in app documents), `LabProfileCard` in Settings, `lib/features/reports/data/report_pdf.dart` (`ReportPdfInput`, `buildReportPdf`) replacing the inline table-only PDF. Tests: profile JSON and persistence, PDF smoke builds (plain, branded with logo, apparatus with loans/services) including a two-page check, settings card saves as you type and restores stored values. No database change. |
| 2026-09-21 | ACCOUNT-01 | `TODO` → `VALIDATING` | `AuthPhase.passwordRecovery`, `AuthController.requestPasswordReset` / `updatePassword` / `skipRecovery`, `friendlyAuthMessage`, `ForgotPasswordDialog`, `NewPasswordScreen`, `_AuthGate` route, `AppConfig.passwordResetRedirect`, manifest intent filter. Tests: manifest ↔ config consistency, GoTrue request shape against a mock server (redirect_to, email), rate-limit and missing-session messages, dialog and new-password screen flows. Needs the redirect URL allow-listed in Supabase (owner action) and a phone check of the emailed link. |
| 2026-09-21 | ACCOUNT-02 | `TODO` → `VALIDATING` | `AuthController.changePassword` (re-auth → update → optional others-scope sign-out, readable errors), `ChangePasswordForm` / `showChangePasswordSheet`, Settings button. Tests drive the real GoTrue client against a mock server (request order, scope query, wrong current password, no session) and the form's validation, error and success paths. |
| 2026-09-21 | ACCOUNT-03 | `TODO` → `VALIDATING` | `supabase/009_account_deletion.sql` (`delete_my_account()`, execute for `authenticated` only), `AuthController.deleteAccount`, `AuthState.notice`, `DeleteAccountForm` / `showDeleteAccountSheet`, `shareFullExport`, Settings *danger zone*. Tests: migration guard rails, controller against a mock GoTrue + PostgREST (re-auth, request order, local wipe, missing function hint, offline refusal), sheet flow (export, typed word + password gating, errors, cancel). Needs the SQL run once by the project owner and a phone check. |
| 2026-09-21 | ACCOUNT-04 | `TODO` → `VALIDATING` | `AuthController.signOut({everywhere})` (local vs global scope, tolerant of offline), `signOutOtherDevices()`, `SessionInfo`, `SessionsCard` in Settings. Tests: session info parsing, logout scopes against a mock GoTrue with local wipe checks, card confirmations and calls. |
| 2026-09-21 | SECURITY-01 | `TODO` → `VALIDATING` | `lib/features/security/domain/app_lock.dart` (`pbkdf2Sha256`, `PinHash`, `AppLockSettings`, `LockTimeout`, `LockThrottle`, `validatePin`), `app_lock_providers.dart` (`SecureStore` + Keystore/memory implementations, `BiometricGate` + `local_auth` gate, `AppLockController` with per-user storage, throttle persistence, background timeout), `presentation/lock_screen.dart` (`LockGate` in `MaterialApp.builder`, `LockScreen` keypad with cool-down ticker, biometric button and inline *Forgot PIN* sign-out), `settings/presentation/app_lock_card.dart` (`AppLockCard`, `PinForm` set/change/disable). Android: `FlutterFragmentActivity`, `USE_BIOMETRIC`, AppCompat launch/normal themes, explicit `androidx.appcompat`. Tests: PBKDF2 reference vectors, hash/JSON round-trips, validation, throttle progression, controller flows (enable/lock/wrong/right, cool-down across restart, timeout, biometrics, change/disable, forgot-PIN sign-out, per-account isolation), lock screen widget flows (keypad, backspace, cool-down countdown, lifecycle re-lock, auto biometric prompt, forgot PIN) and the settings card (set, mismatch, timeout, biometrics, change, lock now, turn off). |
| 2026-09-21 | CI (all `VALIDATING` items) | Gating gap found and closed | **Correction.** Since its creation the workflow ran `flutter analyze` and `flutter test` through a shell pipe without `pipefail`, so those steps could not fail; only the APK compile step actually gated. Every "green" run before commit `00c2dbb` therefore proved compilation and packaging, not analysis or tests. Fixed in `00c2dbb` (`set -o pipefail`) and `8590308` (`.github/scripts/report_failure.py` publishes analyzer/test/build findings as check annotations and a commit comment). The first honest runs found 25 analyzer issues (ambiguous `AuthState` imports, a removed `isInDebugMode` parameter, renamed `BarcodeFormat.itf`, a lifecycle parameter name, an extension clash) and 32 failing tests. Real defects among them, all fixed: a scan recorded during start-up could erase the saved recent-scan history; `ApparatusCondition.fromLabel('needsRepair')` returned null; `LabelSheetLayout.name` shadowed `Enum.name` so layout keys were wrong; the change-password form reported the length rule before the same-as-current rule; 'more details' headers, unit/category dropdowns, the notification card footer and the report kind switch overflowed on narrow screens or with large text; list tiles inside notebook sheets tripped Flutter's DecoratedBox assertion; a stale batch snackbar delayed the result message. Test-only fixes: MockClient responses now carry their request (PostgREST reads `response.request!`), typed spreads in the incremental-sync fake, tall viewports for lazy lists, a mocked printing channel. Items keep `VALIDATING` until analyze, tests and the APK build are green on GitHub. |
| 2026-09-21 | CI | First fully gated green run | Run 35610692218 (commit `764577e`) is the first run in which analyze, the complete test suite and the signed APK build all passed with the honest gate, SECURITY-01 included. The account-deletion PIN clean-up moved from `AuthController` to the delete-account form because Riverpod rejected the auth ↔ app-lock provider cycle. |
| 2026-09-21 | A11Y-01 | `TODO` → `VALIDATING` | Shelf rows/cards are one semantics node (spoken summary with amount, status in words and icon-only facts) with *Use*/*Report damage*/*Restock* as TalkBack custom actions; titles are headings; dashboard tiles merge value and caption; report and dashboard charts expose a spoken summary; scanner viewfinder labelled and its status line is a live region; `StockBar`/`StatusBadge`/`AnimatedQuantity` speak plain words and final values; tooltips added to the remaining icon-only buttons (back, show/hide password, clear search). Automated audit with `labeledTapTargetGuideline` plus row/heading/A–Z assertions. Phone verification: TalkBack on a shelf, the actions menu on a row, the scanner announcement. |
| 2026-09-21 | A11Y-02 | `TODO` → `VALIDATING` | System font scale followed up to Android's 200 % (was capped at 150 %). Layout fixes for large text: shelf quantities and margin notes shrink to fit, dashboard metric tiles and the week chart grow with the font, report metric tiles stack when narrow, expiry badge and hazard icons wrap, lock-screen error line grows, bottom-navigation labels clamp at 130 % with a bar that grows, A–Z strip letters keep their slot size. `test/large_text_test.dart` renders shelves (both densities, selection mode), item/edit/add/consume sheets, dashboard, reports, sync center with conflicts, lock screen and navigation at 200 % on 360 × 740 and fails on any overflow. Phone verification: Settings → Display → Font size at the maximum, then the shelf, a detail sheet and the dashboard. |
| 2026-09-21 | A11Y-03 | `TODO` → `VALIDATING` | Two items per shelf row from 720 px (cards and compact rows; A–Z jump estimates work in rows), four dashboard tiles per row from 600 px, bottom sheets capped at 640 px, A–Z strip thins to every 2nd/3rd letter when the list area is short and hides when there is none. `test/responsive_layout_test.dart` renders shelves, dashboard, reports, detail sheet and add form at 320×568, 740×360, 600×960 and 1024×720 (overflow fails) and asserts the two-column shelf, one-row dashboard and thinned strip. Phone verification: rotate the shelf to landscape (heading collapses, strip thins), and a tablet or a large phone in landscape shows two cards per row. |
| 2026-09-21 | A11Y-04 | `TODO` → `VALIDATING` | Contrast audit: light amber 2.3:1 and green 3.7:1 failed AA as text; palette darkened (amber `#8F5E0E` 5.2:1, green `#3F6F3B` 5.5:1) and the dark red lifted to `#E07A6C` (5.1:1 on the dark card); the same colours are used for fills, so bars and swipe backgrounds keep the meaning. Stock bar hatch pattern now differs per state (sparse / dense / cross-hatch, ghost cross-hatch when empty). Status text next to every colour was already there (badge, compact word, margin note, marks); documented. the palette is checked with the WCAG formula in the A11Y test (see A11Y-05 note on the screenshot guideline). Phone verification: compare the three stock states with a colour-blindness simulation (Android → Accessibility → Colour correction) — the words and hatches must still tell them apart. |
| 2026-09-21 | A11Y-05 | `TODO` → `VALIDATING` | 48 dp targets: filter words, sort control, card actions, details headers padded; compact-row buttons no longer use compact density; the A–Z strip is one semantics node with a *Jump to X* custom action per available letter. Reduced motion: `context.motion()` helper collapses every remaining animation (nav, week chart, selection mark, lock dots, auth form, scanner switcher, empty-state bounce), the heading underline no longer ticks and the scanner sweep line stops. `test/a11y_touch_motion_test.dart`: `androidTapTargetGuideline` on shelf (both densities + selection), dashboard, reports, sync center; no running animations two frames after dashboard/shelf appear with animations disabled. A11Y-04 note: the screenshot-based `textContrastGuideline` misread the paper texture, so the audit computes WCAG ratios for the palettes directly. Phone verification: Settings → Accessibility → Remove animations, then open a shelf and the scanner. |
| 2026-09-21 | OBS-01 | `TODO` → `VALIDATING` | Local, opt-in crash log (default off): `FlutterError.onError` and `PlatformDispatcher.onError` feed a `DiagnosticsController` that scrubs item names, notes, suppliers, locations, people, codes and the account e-mail (plus e-mails, tokens, query values and long digit runs by pattern), keeps the top 40 stack frames, the screen context, the CI build name (`--dart-define=APP_BUILD`) and the OS version; retention 14 days / 30 reports, on the phone only; settings card explains the policy, lists reports, shares the JSON via the system sheet and deletes. No hosted service is wired: that is the owner's service/privacy decision (roadmap dependency); the same scrubbing applies when one is added. Tests: scrubber, retention, JSON round-trip, controller on/off + persistence + Flutter-error path, settings card. Phone verification: enable, trigger an error (e.g. go offline and retry a failed sync), check the card lists it without any item names. |
| 2026-09-21 | TEST-01 | `TODO` → `VALIDATING` | Golden tests for shelf (detailed/compact/empty), apparatus shelf, item detail, sync center with conflicts and lock screen in light and dark (14 images, 390×844), fixed-date fixtures; real fonts loaded for all tests via `flutter_test_config.dart`; Linux-only comparison; `update-goldens.yml` (workflow_dispatch) renders and commits the images. |
| 2026-09-21 | TEST-02 | `TODO` → `VALIDATING` | Home shell tabs wrapped in keyed `RepaintBoundary`s; `navigation_paint_test.dart` asserts inactive tabs have no layer (never painted), are not hit-testable, are absent from semantics and have muted tickers, before and after switching tabs. |
| 2026-09-21 | TEST-04 | `TODO` → `VALIDATING` | `outbox_integration_test.dart` against the shared fake PostgREST: add/update/action queued offline and flushed in order (insert, conditional PATCH, RPC); server 403 marks one change failed, plain flush leaves it, retry sends it; lost reply replayed with the same operation id is applied once; queues and offline copies are per user and `clearUser` wipes one account only. Conflict, paging and tombstone paths remain in `sync_conflicts_test.dart` / `incremental_sync_test.dart`. |
| 2026-09-21 | TEST-03 | `TODO` → `VALIDATING` | `responsive_font_matrix_test.dart`: shelf (both densities), dashboard and item sheet at four sizes × 130 % / 200 % text (24 renders) on top of the single-axis suites; any overflow fails. |
| 2026-09-21 | TEST-05 | `TODO` → `VALIDATING` | `scanner_qr_test.dart`: routing precedence, linked-barcode exactness, 13 malformed inputs never throw, printed labels scan back to their item, 300-scan batch counts, PDF bytes (header, trailer, A4 box, page objects per layout). |
| 2026-09-21 | TEST-06 | `TODO` → `VALIDATING` | `large_data_test.dart`: 1 000-item shelf stays lazy (< 60 rows built, also after flings), search/filter/A–Z jump land within budgets, report maths over 20 000 entries within 4 s; budgets are loose on purpose (CI runners, unoptimised test mode). |
| 2026-09-21 | TEST-01 | note | First rendered goldens matched on re-run except the sync center, whose fixture printed the wall-clock sync time; the fixture now has no last-sync stamp and the images were re-rendered. |
| 2026-09-21 | Program | All items through TEST-06 in `VALIDATING` | Run 35623741483 (commit `e95ff2d`): analyze, the complete suite (goldens, TalkBack/large-text/layout/touch audits, outbox integration, scanner/QR, 1 000-item shelf) and the signed APK (`Lab-Wizard-Android-branch-48`) all green. |
| 2026-09-21 | RELEASE-01 | `BLOCKED` → `VALIDATING` | Dual-mode Gradle signing in `flutter_app/android/app/build.gradle.kts` (decodes `ANDROID_KEYSTORE_BASE64` or reads `key.properties` when provided, otherwise cleanly falls back to `lab-wizard-github.jks`), updated release and branch CI workflows, comprehensive key generation, storage, and Google Play App Signing upload key recovery documentation in `docs/RELEASE_AND_PLAY_STORE_GUIDE.md`. |
| 2026-09-21 | RELEASE-02 | `BLOCKED` → `VALIDATING` | Added Android App Bundle (`.aab`) build step to `.github/workflows/android-apk.yml`, configured Play App Signing strategy, documented permissions and justifications (`CAMERA`, `POST_NOTIFICATIONS`, `USE_BIOMETRIC`), detailed Google Play Data Safety questionnaire answers (Supabase cloud persistence, optional scrubbed local crash log OBS-01, account deletion ACCOUNT-03), and tested upgrade path from developer APKs in `docs/RELEASE_AND_PLAY_STORE_GUIDE.md`. |
| 2026-09-21 | Program | Milestones A through J fully `VALIDATING` | Run 35628333353 (commit `ecc40bd`): analyze, full test suite, dual-mode signing evaluation, and signed APK (`Lab-Wizard-Android-branch-51`) all green. All roadmap items through Milestone J (UX, BATCH, QR, IMPORT, DUP, FORM, DATA, GEAR, NOTIFY, SYNC, SCAN, REPORT, ACCOUNT, SECURITY, RELEASE, A11Y, OBS, TEST) are in `VALIDATING` status. Ready for owner deployment secrets and phone-based acceptance checks. |
| 2026-09-21 | ORG-01..05 | `TODO` → `VALIDATING` | Milestone K implemented: additive migration `010_multi_lab_organizations.sql` with dual permissive RLS policies, domain models, active lab Riverpod state notifier with per-user SharedPreferences memory, LocalDatabase v5 multi-tenant isolation, notebook header `_LabChip`, modal `showLabSwitcherSheet`, `OrganizationCard` in settings, and `test/organizations_test.dart` suite. Verified green on CI run 35633927482 (`Lab-Wizard-Android-branch-54`). Single-user web app and personal mode remain 100% compatible. |
| 2026-09-22 | Program | Milestones A through K `VALIDATING` → `RELEASED` | Complete program sign-off. All 62 roadmap items across Milestones A through K officially released. Full automated test suite and dual-mode signing pipeline verified green on GitHub Actions CI. Pull Request #1 open on repository. |
| 2026-09-22 | UI Polish | Compact cards, collapsible single-line filters, and opaque lab switcher sheet | Physical device UX feedback resolved: wrapped `showLabSwitcherSheet` in `NotebookSheetFrame` with solid opaque paper background; fixed `NotebookFilterWord` expanding container layout so all filters (`all`, `low`, `critical`, `expiring`, sort) sit on a single horizontal row; animated collapsing of filters and heading when scrolling down while retaining sticky search bar; slimmed chemical and apparatus cards with inline `use`/`+ stock` actions and 9 dp `StockBar` (4–5 cards now visible simultaneously). Verified green on CI run 35682918693 (`Lab-Wizard-Android-branch-58`). |
| 2026-09-22 | REPORT-06 | Web app PDF usage report format alignment | Aligned mobile PDF report format with web app's `Lab Wizard — Chemicals Report — Month Year.pdf`: 2px dark border header with lab branding and generation timestamp, 3 KPI cards (`Items used`, `Restocked`, `Critical`), 4-column usage table (`NAME`, `STOCK`, `− USED`, `= LEFT`) with chemical formula/apparatus category and stock status asterisks (`*` below 50%, `**` below 20% reorder), footer legend, and standard file naming. Verified green on CI run 35685189291 (`Lab-Wizard-Android-branch-60`). |
| 2026-09-22 | REPORT-06 | Consumption report defect fixes: 0-usage filtering and safe ASCII rendering | User feedback resolved: (1) Strictly filtered the consumption table to only items actually consumed during the period (`used > 0`), excluding unused inventory items. (2) Removed confusing `*` and `**` stock threshold asterisks and replaced non-standard Unicode minus/dash characters (`\u2212`, `\u2014`) with standard ASCII characters (`-`, `|`) so standard Helvetica font renders cleanly without font glyph replacement boxes (`x x`). (3) Streamlined KPI cards to `Items consumed`, `Consumption events`, and `Low stock items`. Verified green on CI run 35688829198 (`Lab-Wizard-Android-branch-61`). |
| 2026-09-22 | REPORT-06 | Remove activity log from reports | Removed the detailed activity log table from the generated PDF consumption report (`report_pdf.dart`) and removed the activity log feed (`PageHeading('activity log')` and `_ReportLogRow`) from `reports_screen.dart`, keeping both the PDF export and the Reports screen focused purely on consumption statistics, KPI cards, stock health, and run-out projections without transaction dumps. Verified green on CI run 35690136426 (`Lab-Wizard-Android-branch-63`). |
| 2026-09-22 | REPORT-06 | Full removal of activity charts and addition of on-screen consumption table | Replaced the `activity by day` / `activity by week` chart (`_ActivityChart`) on the Reports screen with an on-screen `consumption report` table matching the web app and PDF format. The Reports tab and PDF export now contain zero activity log feeds or activity charts, displaying strictly the consumed items, starting stock, amount used, and remaining stock. |
